# Final local public bundle audit — 2026-09-07

## B48 public journey and sales package — inspection 2026-09-08

The completed local outputs are Node **`daf1207c-adcc-4623-85da-2271911d6fe4`** (preserved directory resolved through `.data/b48-final-node-path.txt`) and Vercel **`900d377f-9c73-463e-8443-4cbef9d226e5`** (`.vercel/output/static`). Exact `latest.json` IDs and the single matching build metadata file were checked before scanning. The scan finished at **2026-09-08 05:06:08.400 UTC**, exit 0. It read **96 JS + 6 CSS per artifact**; all 102 paths and SHA-256 hashes match across builds. All 96 JavaScript ASTs parsed, including 9,379 decoded string/static-template values and 13 decodable percent/base64 representations. There are **1,212 public textual files per artifact**, including **551 HTML and 554 JSON** files. There are no public map files or map references. Binary public assets were inventoried and hashed but not decoded as text.

There were **zero exact canary matches** across 171 listed comparisons: 17 earlier fixture/session values, 42 B48/local browser fixture values, 19 academic test markers, 83 synthetic test email values, 3 private PDF identity tokens and 7 distinct private rate phrases. The rate phrases come from the ignored source table; raw private values are never included in this document or report. No configured provider-secret environment file or genuine provider-secret value was available, so this does not claim a production-secret exact comparison.

All positive signatures were manually attributed. `_nuxt/djuSKq0s.js`, SHA-256 `36129483921f2f0670e7ec3fbee13b074b6324a3d94c886cb973f84446f13852`, contains the question editor's **empty** `correctOptionIds` array and four field accesses for checkboxes/removing options, matching `components/lms/LmsProgramEditor.vue`. No populated key array is embedded at those occurrences. The two numeric coincidences are the editor textarea length limit and numeric input maximum, not source-price records. `_nuxt/DV4hE_NC.js`, SHA-256 `28ccde867c389fc33fde0cfa5dd6795f8f9089c994c71fcbbb8d6959c3369429`, is byte-identical to the earlier reviewed shared context helper: an optional guarded `globalThis.process?.getBuiltinModule?.('node:async_hooks')` capability lookup. It contains no application server-auth implementation or credential. Downloadable authoring UI remains distinct from the separately authorized API data; this static inspection does not replace authorization tests.

Evidence was written under new names without replacing earlier releases: `artifacts/privacy/public-bundle-audit-b48-daf1207c-adcc-4623-85da-2271911d6fe4-900d377f-9c73-463e-8443-4cbef9d226e5-1788843972893.json`, with the manual attribution copy ending in `-reviewed.json`. Helpers are `artifacts/privacy/audit-b48-final-bundles.mjs` and `artifacts/privacy/review-b48-final-bundle-matches.mjs`, both ignored. The bundle manifest SHA-256 is `c63808cdc4284a06264d878935577138a7d0d33617365f8481942020f5a04963` for both artifacts.

This closes the local complete-artifact B50/T029 inspection for these precise outputs with no disclosure found within that scope. It does not inspect a later hosted rebuild/CDN, external scripts, binary PDF content, arbitrary transformations, real learner datasets or provider credentials, and it is not a penetration test or whole-program security proof. The separate actual journey browser results on this Node are recorded in `LEAD_ATTRIBUTION.md`; this scan performs no network calls or runtime writes.

## Lead workflow package — final inspection 2026-09-08

Final Node **`70a16ebb-3355-431a-b18c-7b45f7f23560`** and Vercel **`64ea0c37-1d81-4095-9653-9f7a45160ebf`** each passed 550 public pages, 551 HTML files, 17,327 references, zero missing resources and one build identity. The preserved Node copy includes both final mobile template fixes. The complete audit finished **03:36:20.174 UTC** with **94 JS + 6 CSS**, 100 identical paths/SHA256, 94 parsed JS ASTs and 1,210 public textual files per artifact. Public source maps/references are absent. All **117 canaries** had zero matches: 17 fixture/session, 19 academic, 71 synthetic email, 3 PDF identity and 7 exact private rate phrases. Actual provider credentials were unavailable for exact comparison.

The only positive chunks, `_nuxt/DV4hE_NC.js` (SHA256 `28ccde867c389fc33fde0cfa5dd6795f8f9089c994c71fcbbb8d6959c3369429`) and `_nuxt/DxmIlFk3.js` (`74d3c8c3628eca99b4fcc9f36498e3531535c321ea5896e5dc4bbb99827854f8`), are byte-identical to the manually reviewed c4d9 chunks below. The latter has one empty editor array and four member accesses, with no populated answer bank. Numeric rate coincidences remain in that unchanged editor's form constraints. Evidence: `artifacts/privacy/public-bundle-audit-70a16ebb-64ea0c37.json`, `implementation-lead-final-public-bundle-audit.log`. The earlier 74c7c762/625d3b99 audit at 03:21:47 UTC is retained as a separate prior artifact, with the same 100-file/117-canary scope.

This is an inspection of these complete local build outputs. It does not establish the contents of a later hosted rebuild/CDN or rule out arbitrary transformed secrets. Runtime privacy on final Node70a16ebb separately passed 28 checks, as recorded in `RUNTIME_PRIVACY_AUDIT.md`.

## Analytics and observability package

The final sequential builds are Node **`c4d9bf39-890d-4f44-8562-0075ce696d08`** and Vercel **`05782487-f284-4714-bebc-e8828c7859e3`**. Both passed550 public-page contracts,551 HTML files,16,976 asset references,zero missing resources and one build identity. The Node artifact was copied to an isolated owned directory before Vercel's supported cleanup, preserving the exact inspected files.

The complete public audit finished **2026-09-07 17:06:52.945 UTC**: **93 JS files,6 CSS files,1,209 public text files per artifact**. All99 JS/CSS paths and SHA256 hashes match, all93 JS ASTs parsed, no public source maps/references and no matches among **113 private canaries** (17 fixture/session,19 academic,67 synthetic email,3 PDF identity,7 private rate phrase). No real provider secret was available for exact comparison. Evidence: `artifacts/privacy/public-bundle-audit-c4d9bf39-05782487.json`, command log `implementation-analytics-release-bundle-audit.log`.

Every positive signature was reviewed in context. `_nuxt/DxmIlFk3.js` contains the question editor's empty `correctOptionIds:[]`, checkbox bindings and remove-option filtering: one empty array and four member accesses, with no populated exam keys. `_nuxt/DV4hE_NC.js` retains the same guarded optional `globalThis.process?.getBuiltinModule?.('node:async_hooks')` shared helper. It carries no app server implementation or credential. Privileged editor data remains behind independent API authorization. This is evidence for these exact local artifacts, with the same transformed-secret/hosted limits described below.

Runtime evidence is separate: `RUNTIME_PRIVACY_AUDIT.md` records28/28 actual negative-response/log checks on Node c4d9bf39. Earlier comprehensive UI results name cb3895e4, before the handled-auth401 observation fix; they are not silently relabelled as the final server artifact.

## Additional workflow package: final build inspection

The later audit inspected **Node `4855f35b-6913-4002-a0fa-7c87ad77c0a7`** and **Vercel `ca57b6d3-18bb-45e9-93e2-afaf737785b9`** after the additional organization, reminder and staff workflows. Each artifact contains **89 JavaScript and 5 CSS files**; all 94 paths and SHA-256 hashes match across builds. All 89 JS files parsed, all 1,204 public textual files were scanned, and both build identity checks passed. There are no public source maps or map references. The 102 known-value comparisons (17 fixture/session, 19 academic test, 56 synthetic email, 3 private PDF identities, 7 exact private rate phrases) found no matches. No real provider secrets were configured for an exact production-secret comparison.

Positive signatures remain attributable: `_nuxt/BlOOOiHj.js` contains the authoring editor's empty `correctOptionIds` array and four field accesses; `_nuxt/DV4hE_NC.js` has the same optional shared `async_hooks` helper described below. These are UI/runtime identifiers, without populated academic answers or provider credentials. This result is bounded to the inspected local artifacts, not a claim that all possible transformed secrets or hosted responses were examined.

Evidence: `artifacts/privacy/public-bundle-audit-4855f35b-ca57b6d3.json` and `implementation-bundle-audit-final-4855.log`. The earlier `c76fd093` functional artifact had an extra unused build metadata file from concurrent Nuxt typecheck/build. Its functional browser results remain valid for that artifact, but its strict build identity gate failed. A fresh sequential Node build removed the concurrency issue and passed 550 public pages, 551 total HTML files, 15,797 asset references, missing0 and one build ID. No generated file was manually removed to manufacture a passed artifact.

## Earlier implementation snapshot

The complete local public client assets were inspected at **2026-09-07 14:31:56 UTC**, against Node build **`6aa9fbb6-2c27-4ec9-bb12-8eb4f4fd60f9`** (`.output/public`) and Vercel build **`34a99837-1e71-4025-83a9-2414427497cf`** (`.vercel/output/static`). Both expected build IDs were confirmed. Application runtime source was unchanged during this audit; the source commit was `1e23fc5`.

Each artifact contains **84 JavaScript files, 762,621 bytes**, and **5 CSS files, 25,314 bytes**. Every file was read, hashed and scanned; all 89 assets have identical paths and SHA-256 hashes between the two builds. All 84 JavaScript files parsed successfully with Acorn. Inspection included 7,044 decoded string/static-template values and 13 decodable percent/base64 representations per artifact. There were **no public `.map` files or `sourceMappingURL` references**, including inline data maps. The scan also covered all **1,199 public textual files per artifact**, including 551 HTML files and 554 JSON files, so static HTML and Nuxt payloads were included.

Exact comparisons used 17 local synthetic fixture/session canaries, 19 distinctive test academic-content canaries, 39 synthetic `example.test` email addresses, 3 private source-PDF identity tokens and 7 exact private rate phrases. **None matched.** Values were read locally and are not recorded in the evidence or this document. No real provider-secret environment file or configured provider-secret value was available to this audit; it does not claim an exact comparison against production credentials.

All positive signatures were reviewed:

- `_nuxt/WqLWID9i.js` contains five `correctOptionIds` occurrences from `components/lms/LmsProgramEditor.vue`: one property initialized to an **empty array**, and four member accesses for the editor checkbox binding and removal/filter handling. Its new-question template has empty question/option text and generated IDs. This is expected authoring UI code; no populated answer-key array or test question bank was embedded. Actual authored data is obtained from the protected admin API. Source review confirms the handler requires a user and `listVersions` requires the editor/reviewer role through the server role/MFA guard. A public download of an editor chunk is not an authorization mechanism, and this audit does not treat it as one.
- `_nuxt/DV4hE_NC.js` contains an optional `globalThis.process?.getBuiltinModule?.('node:async_hooks')?.AsyncLocalStorage` lookup in a shared context helper. It is not an import of the application authentication service or a bundled auth/database/mail server SDK. The Node-specific capability lookup is guarded and carries no credential or private payload. Other tested server-SDK, server-implementation and private-env signatures had no matches.
- Two numeric coincidences with private source rates occur only in the editor's textarea length limit and numeric input maximum. Their surrounding code corresponds to existing form constraints. Exact private rate phrases and PDF identity tokens had no matches; numeric equality alone is not evidence of a price disclosure.

The command `node artifacts/privacy/audit-final-bundles.mjs` completed with exit 0. Redacted evidence, complete file hashes and the inspected AST occurrences are in ignored `artifacts/privacy/public-bundle-audit-6aa9fbb6-34a99837.json`. The one-off inspection script is also ignored. No application or reusable QA source was changed for this inspection.

This completes the **full local artifact inspection portion of B50/T029**, alongside the separately recorded DTO, role and browser tests. It is evidence for these two exact artifacts, not a security proof derived from regex absence. It does not include a hosted rebuild/CDN, external scripts, arbitrary encodings, a whole-program information-flow analysis, a penetration test, or a new runtime authorization test. No real learner PII dataset or production secret collection was available; canary coverage is limited to the explicitly listed local inputs. A hosted deployment with a different build identity requires its own artifact or response inspection before extending this conclusion to production.
