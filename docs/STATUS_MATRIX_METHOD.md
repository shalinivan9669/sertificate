# Implementation and acceptance status matrix

Snapshot date: **2026-09-08, after actual production a304ae5 publication and the 07:23:50 UTC public smoke**. All **58 backlog requirements and 84 acceptance cases** remain: **40 passed, 0 failed, 13 blocked and 5 not_run backlog rows**; **75 passed, 0 failed, 4 blocked and 5 not_run acceptance rows**. This evidence-only update changes no status. The website is published; composite authenticated learning, provider, content and recovery acceptance remains incomplete.

## Source preservation and method

The original `OT_CENTER_CODEX_HANDOFF_2026-09-07/data/backlog.csv` and `data/acceptance_cases.csv` remain unchanged. The two status CSVs preserve every source ID, row order and source-column value. Only the original `status` heading is renamed `source_status`; four appended columns contain `implementation`, `status`, `evidence` and `limitations`.

The bundled Artifact Tool imports the sources, authors the matrices and checks them before UTF-8 CSV export with BOM, comma delimiters and quoted multiline-safe fields. Validation compares all source cells, all 58/84 unique IDs, permitted statuses and a full import/export round trip. No formulas, account credentials, raw personal records, verification tokens, PDF contents or private source prices are introduced. The final export passed these checks; rendered review samples cover changed B15/B51/B56 and T081/T082/T084 rows, with compact implementation/limitation views for legible review. Full text remains in the CSV.

- `passed`: the requirement was actually verified at its stated service, HTTP, browser or artifact scope, with limits identified.
- `failed`: a listed check ran and remains unsuccessful.
- `blocked`: missing external configuration, approved content, human review or provider capability prevents completion; implemented portions remain described.
- `not_run`: the exact composite scenario or required final scope is incomplete, even when narrower related checks passed.

Counts describe requirements, not tests. Several tests may support one case; one browser sequence may support several cases. Historical source statuses are never counted as implementation results.

## Current production publication — a304ae5

[PR2](https://github.com/shalinivan9669/sertificate/pull/2) is merged into main **a304ae5b28b61cb7b2c2a6f5fbef566f4451a7cb**, with the application tree identical to green b7b7bf9. [Vercel4Tqx1LxVvN3sgWtgo4D9LEnnfZwH](https://vercel.com/shalinivan9669s-projects/sertificate/4Tqx1LxVvN3sgWtgo4D9LEnnfZwH) is **Ready, Production, Current**, completed **06:16:34 UTC**, 1m33s, on the existing **Hobby** plan. The public site is [www.otcenter.kz](https://www.otcenter.kz). No paid plan upgrade was performed.

The exact main checkout passed [Quality34193839442](https://github.com/shalinivan9669/sertificate/actions/runs/34193839442), job101957211285, **06:14:58–06:23:18 UTC**: source **207/207**, failed/cancelled/skipped0, **28.854537161s**; lint/typecheck and audit0 passed. Core6 suites, analytics20, privacy35 (**13.452s**), journey10 and workspace9 all passed. Linux Node **28f227fc-c841-433c-92e8-3d234c27039a** (29.3MB/9.01MBgzip) and Vercel **bb312593-db75-4511-a7fd-7e10b7a4e232** (29MB/8.96MBgzip) each passed550 public/551HTML/18,110refs/missing0/oneID. These are CI artifact IDs, separate from the hosted deployment.

Independent public production smoke **8/8 passed, exit0**, completed **07:23:50.075 UTC**, recorded in artifacts/production-public-smoke-a304ae5-report.json. Homepage200 has canonical www; health200/ok, readiness200/ready, auth configuration available=true and emailDeliveryConfigured=false, analytics disabled/v2, catalog storageAvailable=true with20 request/null-price directions and0 published versions. Apex contacts307 retains the exact path/query and its canonical target responds200. All five API responses are private/no-store/max-age0, noindex/nofollow/noarchive and nosniff. Only positive public GETs were sent, without cookies, credentials, POST, cron, registration or external delivery. Root additionally viewed the20 catalog cards and RU/KK direction-to-contacts context without submitting a form.

Production Turso, stable Auth/cron secrets and canonical origins were configured before this deployment. CRM delivery is enabled using existing settings; **real CRM delivery is not verified by the GET smoke**. Preview has a separate Auth secret/origin, staging/CRM0/noindex and email/alerts0. **Email remains OFF by explicit owner choice**, with no SMTP credentials awaited. Auth configuration availability does not prove a real verified-user session; queued mail is not sent mail. Real payments, invoices and analytics remain disabled. No verified accounts, exams or credentials were inserted as production fixtures.

The current public release does not close B15/B18/B33/B35/B47/B51/B53/B55/B56/B58 or T017/T079/T084 composite scopes. Remote concurrency/remote restore/RTO-RPO, two-user CDN isolation, real authenticated learning, cron execution, provider delivery, approved academic content/templates and human review retain their recorded limits. **The later disabled-mail outbox fairness correction is being handled separately and is not deployed in a304ae5**; neither its fix nor delivery acceptance is claimed by these matrices. The historical sections below retain their original dates and evidence scopes.

## Historical publication and infrastructure — b7b7bf9

Published **b7b7bf9feaf9b53c40ac16f67742faac21ac7846** has [Quality34192178489](https://github.com/shalinivan9669/sertificate/actions/runs/34192178489), job101952313353, **success**. The job ran **05:50:31.161–05:59:00.735 UTC**, **509.573 s**, against PR merge81bb75e. Source **207/207**, zero failures/skips, **32.804615528 s**; lint/typecheck and audit0 passed. The complete six-suite core (classic8/auth6/contact2 locales/context8/learner11/staff13), analytics20, privacy35 (**15.391 s**), journey10 and workspace9 all passed. These are actual completed Linux results supplied by root from the job, not extrapolation from local focused tests.

Linux Node **d86c1f99-245e-49c0-a159-7b2238dc9490** and Vercel **c68324cb-f2da-4780-b5a1-96d424e9f0bd** each passed550 public/551HTML/18,110 references/zero missing/one build ID. Node29.3MB/9.01MBgzip and Vercel29MB/8.96MBgzip refer to these Linux artifacts. The complete canary audit below still names its separate local daf1207c/900d377f artifacts; it is not a hosted-CDN audit.

Earlier B48 Quality34190228369 for4ed8ca9 **failed** after204 source tests, core6, analytics20 and privacy35: the journey observer encountered SQLITE_BUSY after five checkpoints. Quality34191204736 forf7a3e77 **failed** after207 source tests, core6, analytics20, privacy35 and journey10: workspace stopped after four checkpoints on datetime-local normalization before proposal POST. Their skipped later steps are not passed. Bounded read-observer retries and exact datetime normalization changed only tests; green b7b7bf9 includes their verified repeats.

The prior4ed public preview [BNbKnWMziKzFDTjMrTv5o4KPi1Wq](https://vercel.com/shalinivan9669s-projects/sertificate/BNbKnWMziKzFDTjMrTv5o4KPi1Wq) was **Ready in1m22s at05:21:27 UTC**. Root viewed20 request-price directions, privacy-v2 collection disabled and RU contacts/KK B2B query prefill without sending forms. This was the public preview scope before production; the subsequent actual production release is recorded above.

The owner confirmed account use and connected **database-sky-village** to Production+Preview, with preview branches enabled and Development off. Three pre-migration fresh read-only clients reported FK1 and an empty database (1863/338/350ms, root-observed). Exact committed Git LF migrations001–012 fromf7a3e77 completed **05:46:26.919 UTC**, exit0. The checked readiness log confirms three independent fresh web clients, schema12, FK default/check/negative probe passed and **writesCommitted0**. A future preview branch still needs its own actual schema/readiness verification.

Root's actual encrypted remote export and new-local restore both passed. Independent read-only comparison finished **06:00:47.364 UTC**:12 exact migration names/checksums;49 tables/32 rows; programs20/users0/programVersions0/publishedVersions0/leads0 in both databases; all table counts and schema SHA equal. Local integrity and both FK checks passed; the local file was opened read-only and its SHA remained unchanged. Verification took2,736ms (remote2,720/local7), **not** a measured backup/restore duration. Private evidence remains under .data/remote-rollout-fb768200-9b0f-4023-b075-9c57e30be84d, including read-only-restore-verification-3ee7e2d8-38f9-43ef-b528-4b178f802c58.json; no credentials/records are copied here. This is **remote export→LOCAL restore**, not remote recovery, RTO/RPO, a populated production restoration or old-runtime rollback.

**Email is explicitly OFF by the owner's choice.** SMTP credentials are not being awaited or requested. Queued messages do not establish delivery or verified identity; real verification/reset email and external alert acceptance are not claimed. At this historical snapshot Auth and hosted application setup were still in progress; current public configuration/readiness is recorded above. Real payments, mass notifications, approved academic content/templates, external provider delivery, remote recovery and composite production gates are not upgraded by these facts. All matrix statuses remain unchanged.

## Historical local B48 acceptance — before publication


At this historical local snapshot the package had not yet been committed. Its compiled browser evidence uses Node **daf1207c-adcc-4623-85da-2271911d6fe4** and is distinct from published 39243b6, the historical local 70a16ebb artifact, and the unsuccessful intermediate artifacts below. **B48 passes at its actual local implementation scope** after its own journey, staff workspace, analytics and privacy checks. Remote CI and preview had not yet run at that historical snapshot; their subsequent actual results appear above.

| Scope | Verified result so far | Evidence and limits |
| --- | --- | --- |
| Complete source suite before the final narrow browser-discovered corrections | **203/203 passed**, zero failures/skips, **59.972 s**; complete lint, final typecheck and dependency audit with zero vulnerabilities passed. | Root-confirmed `implementation-b48-*` logs. Test-only transaction/UUID type corrections were followed by final typecheck. A later new client regression passed **9/9**; this is **not a claimed full 204-test run**. |
| Server sales and cohort logic | **18/18 passed**, zero failures/skips, **16.810 s**: 13 new sales scenarios plus five prior cohort scenarios. | `artifacts/sales-links-tests.log`, `docs/SALES_FUNNELS.md`. Actual local learning/assessment/PDF, signed sandbox payment/refund, two-seat invoice confirmation/fulfillment, concurrent claims, audit rollback, current access/membership, one-enrollment chain, bounded read-only report, selection/auth observations. No real providers. |
| Optional attribution and CRM context | **33/33 passed**, **19.644 s**: 11 server, eight client and 14 CRM-integrity tests. Later source-only duplicate-context regression: **9/9 client tests passed**. | `artifacts/lead-attribution-focused-final.log`, `artifacts/lead-attribution-client-dedupe.log`, `docs/LEAD_ATTRIBUTION.md`. Strict v2 consent, bounded per-tab observations, immutable pre-accept snapshots, current expiry and server-only auth confirmation; local injected CRM transport. |
| Populated additive upgrade and observability | **005/010→012: 2/2 passed**, **4.265 s**; focused operational failure counters **9/9 passed**, **17.109 s**. | The first upgrade cleanup failed only on Windows native-file lifecycle after successful data assertions; child-process isolation fixed cleanup. No migration001–010 rewrite. Operational counts describe HTTP failures, not exam outcomes. |
| Final compiled public journey | **10/10 passed**, exit 0, zero JavaScript errors, completed **05:00:35.846 UTC**. | `artifacts/lead-attribution-browser/4182ecdc-8665-486c-bb41-85dcd201c529/report.json`, Node daf1207c. Actual opt-in, ordered entry/program/consultation, actual accepted form, immutable snapshot/replay, separate opener-cloned tab, KK withdrawal, genuine RU matched/KK unmatched and verified-auth confirmation. Both PNGs were reviewed. External delivery disabled. |
| Final compiled staff workspace and report | **9/9 passed**, exit 0, zero JavaScript errors, completed **04:59:24.107 UTC**. | `artifacts/lead-workspace-browser/9bc36998-5bbc-4f0b-b189-b034e662291f/report.json`, Node daf1207c. Actual hard-loaded RU/KK context, two accepted forms, admin password/TOTP, lost-response replay, exact links/proposal/assignment, foreign-org409, learner403, unchanged payment/academic state and report denominators. All three PNGs reviewed. Optional browser tracking disabled in this separate workspace run. |
| Final complete core browser run | **6/6 suites passed**, exit 0, completed **05:04:55.144 UTC**. | `artifacts/core-browser/1c6f1339-e719-4c78-9d62-261205b57989/report.json`, Node daf1207c: classic8/auth6/contact2/leadContext8/learner11/staff13 on one separate synthetic database. This is one actual successful six-suite run, distinct from prior local split runs. |
| Final analytics regression | **20/20 passed**, exit 0, zero JavaScript errors, completed **05:03:24.578 UTC**. All five PNGs reviewed. | `artifacts/analytics-browser/edba1510-bb8a-44ba-80e5-8c6aedc6508a/report.json`, Node daf1207c. Original eight event types/12POST/12UUID, v2 consent/revocation, admin TOTP and19 event rows, windows, learner denial and two real local HTTP leads/delivery cohort remain checked. |
| Final expanded HTTP/log privacy sample | **35/35 passed**, exit 0, zero failures, **63.758 s**, completed **05:05:40.488 UTC**. | `artifacts/privacy-runtime/e357844b-6752-4ec1-897a-2a2db4678b23/report.json`, Node daf1207c. Nineteen selected negative HTTP responses produce19 API-error counts; autosave2,21 closed-schema logs,14 private-canary categories with zero matches, empty stderr. New journey/auth schema, disabled/no-consent gates, forged confirmation refusal, anonymous401 and actual server confirmation timestamp are covered. No real provider/SMTP/hosted logs. |
| Final local artifacts | Node **daf1207c** and Vercel **900d377f-9c73-463e-8443-4cbef9d226e5**, both build exit0; **550 public pages /551 HTML /18,110 asset references /zero missing resources /one build ID each**. | No private prerender. Vercel function19.3MB/4.76MBgzip, Node24/max60s. These generated artifacts are not hosted production verification. |
| Complete final public-bundle audit | Audit completed **05:06:08 UTC**, manual review **05:09:29 UTC**: **96JS+6CSS**,102 identical paths/SHA256,96 AST parses per artifact,1,212 public text files each including551HTML/554JSON,171 private-canary comparisons and **zero findings**, no maps. | `artifacts/privacy/public-bundle-audit-b48-daf1207c-adcc-4623-85da-2271911d6fe4-900d377f-9c73-463e-8443-4cbef9d226e5-1788843972893-reviewed.json`, `docs/PUBLIC_BUNDLE_AUDIT_2026-09-07.md`. Manual review attributed djuSKq0s empty correctOptionIds/four staff-editor accesses/two numeric constraints and unchanged DV4hE_NC optional async_hooks. Real provider values, hosted CDN and binary PDF contents are outside this sample. |

The browser tests found actual application defects and were not weakened to count them as passes. Node f7dac4e1 exposed i18n code26 because a Nuxt plugin invoked a component-only composable; the plugin now uses its actual injected composer. Node ca44595e exposed duplicate home steps when only source changed; typed context deduplication preserves the original source, with a new regression. Final plugin startup waits for Nuxt readiness so prerender hydration has restored the real URL query. The strengthened workspace pilot also checks hard-loaded form context after the narrow hydration/prefill correction. Earlier failed/intermediate reports remain in `LEAD_ATTRIBUTION.md` and `LEAD_WORKSPACE.md`; they are not relabelled as daf1207c passes.

The implemented `sales` report separates consented per-tab journeys, accepted B2C leads and explicitly registered sent B2B proposals. Staff qualification and selected order/enrollment/invoice fulfillment links are audited, idempotent and protected by current role/MFA. It does not merge contacts or use client success as a payment, grade or credential fact. Cohort windows are `[from,until)`, state is current at `asOf`, stage denominators are the preceding unique units, and later learning stages require one complete enrollment chain. Oversized windows are explicitly incomplete. The original eight client/eleven server event populations and `conversionRate=null` remain separate.

External search queries, CPA and market share remain unavailable; stale-material age is null without an approved review deadline. These missing inputs are not replaced with zeros or growth claims. Production privacy/region, actual DB/auth/mail/provider/CRM configuration, hosted cleanup and observation after rollout remain separate **B18/B33/B55/B56 blocked**, **B58 not_run**, and **T084 blocked** gates. Owner production-deployment permission already exists; no legal-entity permission check is reopened. T076 retains passed at its new actual35-check local sample scope; B50/T029 retain passed for the complete final local artifacts. This does not close hosted delivery, CDN isolation, field CWV or production cutover.

## Historical published CI and preview — 39243b6, preceding B48

Published commit **39243b621123a3d4b13b01437984f8a992f37722** has [GitHub Quality 34184595871](https://github.com/shalinivan9669/sertificate/actions/runs/34184595871), job `101930273920`, **completed/success at 03:52:28 UTC on September8**. Completed logs confirm install/lint/typecheck/audit, **169/169 tests**, zero failures/skips, **25.972 s**, both builds and the complete **six-suite core run** on Node `fe1872d2-1855-458c-9e02-986c5920afee`: classic8/auth6/contact2/leadContext8/learner11/staff13. Its core report is `artifacts/core-browser/b8f9bbf4-cff5-42d5-ba5a-b5ffe4e1fbab/report.json`, completed03:51:12 UTC. Analytics **20/20** completed03:51:28; privacy **28/28**, **14.444 s**, completed03:51:43. These are published preceding-package results, not the new B48 UI/services.

Node fe1872d2 and Vercel `953f76b4-b434-4d99-afcf-4d9b9e5c1cba` each had **550 public pages / 551 HTML / 17,327 references / zero missing resources / one build ID**. The Linux function size28.6MB/8.9MBgzip differs from the local Windows artifact and is not an interchangeable measurement. Successful CI artifact files are not uploaded by this workflow; exact run paths/counts were verified from logs. The complete private-canary scan still names its separate local artifacts, not this hosted CDN.

[Vercel deployment 3Em2KTZ9xR5XSTuhcdkXUNjmb67r](https://vercel.com/shalinivan9669s-projects/sertificate/3Em2KTZ9xR5XSTuhcdkXUNjmb67r) was **Ready in1m24s at03:46:37 UTC**. Root viewed all20 request-price directions in [its published catalog](https://sertificate-5illu0gjs-shalinivan9669s-projects.vercel.app/courses). The protected public preview does not establish hosted DB-dependent operation, real CRM delivery or production promotion; main remains unchanged.

## Historical lead context, CRM integrity and cohort package — local checks before 392 publication

This historical local package followed commit `393c5630b87147a60394f830150a2c19622673e2`. Source regression, individual browser runs and artifact audits were recorded separately. **Those local runs do not establish one successful six-suite run or all six suites on final Node70a16ebb.** At that earlier snapshot the package was not yet committed or checked by remote CI; its subsequent published392 six-suite result is recorded above.

| Scope | Actual result | Evidence |
| --- | --- | --- |
| Complete source regression | **169/169 passed**, zero failures/skips, 49.96 s; full ESLint passed. Initial typecheck found only types in new tests. Corrected full typecheck passed, with an additional **19/19** affected-test run, 14.992 s. | `implementation-lead-release-tests.log`, `implementation-lead-release-lint.log`, `implementation-lead-release-typecheck-final.log`, `implementation-lead-release-typefix-tests.log`. The later narrow hidden-honeypot/nowrap markup changes have the compiled evidence below. |
| CRM integrity and atomic delivery | **14 new CRM + 27 business tests = 41/41 passed**, 15.636 s. Positive safe IDs, strict provider shapes, bounded full lookup, ambiguous note recovery and atomic delivered/audit commit were checked with isolated libSQL and a controlled transport. | `artifacts/crm-response-integrity-tests.log`, `docs/CRM_RESPONSE_INTEGRITY.md`. Genuine audit INSERT failure preserves `note_pending`; retry recovers the existing note without another POST. No real CRM calls. |
| Lead-context and cohort services | **6 context tests**, including 900 direction/city/format combinations; **5 cohort integration tests**, including a single indexed query for 600 rows and 26 corrupt historical records. | `tests/lead-context.test.ts`, `tests/lead-cohort.test.ts`, `artifacts/lead-cohort-tests.log`, `docs/LEAD_CONTEXT.md`, `docs/ANALYTICS_SERVER.md`. Source tests are not 900 browser journeys. |
| Earlier Node `74c7c762-fa2a-45f4-bb9a-ad844f14ff29`, first attempt | Classic 8, auth 6 and contact 2 locales passed. The new context suite then failed because its city locator matched two controls. | `implementation-lead-core-browser.log`. This is a partially successful run followed by a test-harness failure, not six successful suites. |
| Same 74c7 Node, corrected remaining-suite run | Context 8, learner 11 and staff 13 passed; finished **03:24:17.171 UTC**. | `artifacts/core-browser/201d9b8a-fd18-4106-8170-d59c5a4f6c85/report.json`. Actual UI review then found a visible B2B honeypot; that application defect required a new build. |
| Final Node `70a16ebb-3355-431a-b18c-7b45f7f23560`, targeted context repeat | **8/8 passed**, exit 0; finished **03:29:58.337 UTC**. Four RU/KK mobile screenshots were viewed. The honeypot is now hidden and explicitly asserted hidden. | `artifacts/core-browser/c415505f-bad0-42fb-9b0c-b810beb985c9/report.json`. This run contains only the context suite. Its intercepted `amo-lead` responses prove UI payload/retry behavior, not CRM delivery. |
| Final Node 70a16ebb, analytics and lead cohort | **20/20 passed**, exit 0; finished **03:30:18.400 UTC**. The original 16 consent/event/admin checks remain, followed by two genuine local accepted leads and current cohort checks in RU/KK at 360px. Both screenshots were viewed; the count-label wrap defect is fixed. | `artifacts/analytics-browser/fa0632b7-7d1b-4d08-b0a2-c7aee6360dd5/report.json`. Totals 2 accepted / 0 delivered / 2 pending, one B2C and one B2B; no contacts or IDs in cohort DTO/UI. External CRM delivery disabled. |
| Final Node 70a16ebb, compiled privacy sample | **28/28 passed**, exit 0, **35.619 s**, finished **03:36:30.835 UTC**. Fifteen selected negative responses produced fifteen API-error increments; two autosave failures, 17 closed-schema logs and zero private-canary matches in 14 categories. | `artifacts/privacy-runtime/e1b2bc96-b58e-4e7c-a498-7935db56260e/report.json`. Successful owner auth/learning bodies are excluded from the negative-response scan; their private canaries are still checked in stdout. |
| Final local Node 70a16ebb and Vercel `64ea0c37-1d81-4095-9653-9f7a45160ebf` | Both builds exited 0. Each contains **550 public pages, 551 HTML including GSC, 17,327 asset references, zero missing resources and one build ID**. No private prerender. | `implementation-lead-final-node-build.log`, `implementation-lead-final-vercel-build.log`. The earlier 74c7/625d3 artifacts are not renamed as these final builds. |
| Complete final public-bundle audit | At **03:36:20.174 UTC**, all **94 JS + 6 CSS files**, 100 paths/SHA256, matched between the two artifacts. All 94 JS files parsed; 1,210 public text files per artifact and **117 private canaries, zero matches**; no maps. | `artifacts/privacy/public-bundle-audit-70a16ebb-64ea0c37.json`. Root reviewed exact unchanged SHA256 of the two previously attributed benign editor/optional-context chunks against c4d9; `positiveChunkComparison` records this evidence. Real provider-secret values and hosted CDN files remain outside scope. |

**At this historical snapshot B48 was not_run for the full attribution scope.** The new `leadCohort` counted the same `lead_submissions` created in the exact report window `[from,until)`, regardless of optional telemetry collection. Its unit is `accepted_leads`; B2C/B2B/unknown groups have current states at the labelled `asOf`, not a reconstruction of status at a past window boundary. `accepted = delivered + pending + invalid`; `notePending` is a subset of pending. Delivery requires both positive safe CRM IDs and the delivered state. Invalid records remain in the denominator, and the delivery rate is null when it is empty. This is a coherent delivery fraction, not visitor conversion. First/last-touch linkage and qualified linked funnels were still unimplemented at that time; the new B48 section above describes their subsequent implementation and outstanding verification. Existing event populations and `conversionRate=null` are unchanged.

**B53/T083 remain not_run for functional application rollback.** The reproducible current-schema drill now copies `requiredMigrations` and uses fixture services from the same checkout. The actual **001–010 run passed 14 checks**, preserving **43 tables / 50 rows** through snapshot, restore and migration replay. Unchanged legacy preflight rejects unknown migration010 even on a control-free copy: **old runtime not started, legacy HTTP requests 0**. Eight negative copies retain their own specific blockers. Evidence: `artifacts/rollback/report-2026-09-07T17-35-11-563Z.json`, `docs/ROLLBACK_DRILL.md`. This successful refusal is not a working old-writer rollback. The historical **001–009 run with 15 checks and six old-runtime protected GET requests** remains separately identified below.

**B50/T029 retain their local-artifact passed scope; T076 retains its actual compiled-sample passed scope.** New bundle and privacy audits do not establish hosted/CDN isolation, field INP/CWV, active-exam capacity, actual external delivery or production readiness. B52 external alert/SMTP/hosted scheduling dependencies and T079 screen-reader smoke remain incomplete. Old performance, keyboard, document, organization and extra-acceptance evidence keeps its original build and scenario scope.

## Historical published CI and preview — 393c563

[GitHub Quality 34147961390](https://github.com/shalinivan9669/sertificate/actions/runs/34147961390), job `101823966080`, completed successfully on **2026-09-07 at 17:38:34 UTC** for commit `393c5630b87147a60394f830150a2c19622673e2`. Linux install/lint/typecheck/audit, **144/144 tests with zero failures/skips in 24.39 s**, five core suites (classic 8/auth 6/contact 2/learner 11/staff 13), analytics **16/16**, runtime privacy **28/28 in 14.401 s**, Node and Vercel builds all passed. Node `3e77ec63` and Vercel `745651ef` each had 550 public / 551 HTML, 16,976 references, no missing assets and one build ID. Results were confirmed from completed CI logs; successful CI artifact files are not uploaded by that workflow. This preceding 144-test result is not the new local 169-test packet's remote CI.

The preceding [run 34146794421](https://github.com/shalinivan9669/sertificate/actions/runs/34146794421) failed at the analytics launcher because it unconditionally used a Windows Chrome path on Linux. Its earlier tests/core stages passed, but later privacy and Vercel steps were skipped. Commit393 fixed browser selection and the complete subsequent run above passed. The failure remains part of the record.

[Vercel deployment 7eofGuRHnMzmTgR6ZSPBTEF7wrSg](https://vercel.com/shalinivan9669s-projects/sertificate/7eofGuRHnMzmTgR6ZSPBTEF7wrSg) was **Ready in 1m20s**, at **2026-09-07 17:32:49 UTC**, for the published 393 package. Root actually viewed its [20-direction catalog with request-only pricing](https://sertificate-kojoaqgcz-shalinivan9669s-projects.vercel.app/courses). It is a protected preview, not authenticated service/readiness or CRM delivery proof. The new local package is not included in that deployment.

Production remains unpromoted; the read-only fetch on September8 at 03:25 UTC still found `main` at audit commit413ad4a. Owner deployment permission is recorded separately from configuration readiness. The new public `amo-lead` handler reaches its persistent database limiter before accepting or queuing a lead; deploying it without Turso configuration returns `DATABASE_NOT_CONFIGURED` and would break the existing public intake path. Persistent DB/schema/FK, auth/mail and controlled real CRM delivery verification are therefore concrete cutover dependencies. No SaaS terms, real payments or bulk notifications were accepted or executed by these checks. Confirmed legal permissions are not being re-audited.

## Previous analytics and observability package — 2026-09-07 before the 393 CI result

The source test, browser, compiled-runtime and artifact checks below are separate evidence. A successful source test is not relabelled as a hosted or browser run.

| Scope | Actual result | Evidence |
| --- | --- | --- |
| Complete source regression after the handled-auth-status fix | **144/144 passed**, zero failures/skips, 38.84 s, exit 0. Full lint and final Nuxt typecheck passed. | `implementation-analytics-release-tests.log`, `implementation-analytics-release-typecheck.log`. The preceding 143-test run predates the additional 401 regression. |
| Five browser suites, Node `cb3895e4-c572-4c0d-8891-2345381742f4` | Classic 8, auth 6, contact 2 locales, learner 11, staff 13, all passed at 16:59:27 UTC. Separate synthetic DB and disabled external delivery. | `artifacts/core-browser/f11f680f-e518-4640-aaa6-0205bc80f4df/report.json`. This artifact precedes the narrow handled-auth logging fix. |
| Client analytics and administrator report, Node cb3895e4 | **16/16 passed**: all eight client event names, consent/no-consent/revocation, allowed payloads, actual admin sign-in/TOTP, 19 report rows, UTC retention boundaries and learner denial. | `artifacts/analytics-browser/2bd3d1af-f545-4e05-bfa5-550b22aafd59/report.json`, `docs/ANALYTICS_CLIENT.md`. |
| Compiled HTTP/log sample, final Node `c4d9bf39-890d-4f44-8562-0075ce696d08` | **28/28 passed**, exit 0, 36.841 s, completed 17:03:09 UTC. Fifteen negative responses produced fifteen `api_error` increments, including actual auth 401; two autosave failures and 17 closed-schema logs were observed. No private canaries in 14 categories appeared in the checked samples. | `artifacts/privacy-runtime/6d6e3e21-a4e0-492b-a73e-d3157667271b/report.json`, `docs/RUNTIME_PRIVACY_AUDIT.md`. |
| Final Node c4d9bf39 and Vercel `05782487-f284-4714-bebc-e8828c7859e3` | Both builds exited 0. Each contains **550 public pages, 551 HTML including GSC, 16,976 asset references, zero missing resources and one build ID**. No private prerender. | Build checks recorded in `IMPLEMENTATION_STATUS.md`; current artifact audit below. |
| Complete final public bundle audit, 17:06:52 UTC | All **93 JS and 6 CSS files**, 99 identical paths/SHA256 per artifact; 93 AST parses, 1,209 public text files and **113 exact private canaries with zero matches**. No maps. | `artifacts/privacy/public-bundle-audit-c4d9bf39-05782487.json`, `docs/PUBLIC_BUNDLE_AUDIT_2026-09-07.md`. |

The initial compiled runtime sample on cb3895e4 was **25 passed / 1 failure**: Better Auth correctly returned 401, but H3 still exposed event status 200 before unwrapping its Web Response, so the observer missed that failure. This was missing telemetry, not evidence of a secret leak. The wrapper now copies only the returned status into the event and returns the original Response. A real wrong-password test preserves the SDK body and cookie behavior while requiring one safe log/counter. The focused 19-test suite passed, and the new compiled 28-test run closed the original failure. Failed evidence remains in its original report.

**At this earlier September7 snapshot B48 was changed to not_run for its full scope.** Eight optional client events and eleven confirmed server event types were connected to actual actions, with strict dimensions, deduplication, retention and an MFA-protected aggregate report. Server transitions used committed domain/audit facts; actual 2-seat and 100-seat invoice confirmations produced one payment event per invoice and one activation per seat, including replay and retention tests. Optional analytics failures did not roll back learning or mandatory audit. Full landing→consultation→CRM attribution and its funnel were still missing then. Different browser and server populations were labelled explicitly, with `conversionRate=null`; the aggregate was not declared complete attribution. The remaining engineering scope was subsequently implemented in the current B48 package above, rather than attributed to a missing external analytics account.

**B52 remains blocked for complete production operations.** Migration 010 adds nullable request/correlation metadata to audit/outbox history. Generated IDs and `AsyncLocalStorage.run` isolate concurrent HTTP operations and per-job delivery spans; closed structured logs, controlled error bodies and bounded API/autosave counters passed source and actual compiled checks. External alert recipient/channel, SMTP, hosted scheduling and production failure injection remain unconfigured/unverified. **T076 passes at its actual local sample scope**; real provider/SMTP/hosted logs were not examined. Successful owner auth/learning DTOs are excluded from the negative-response scan, while their private canaries are still searched in stdout.

**B50 and T029 pass for the new local artifacts.** Manual review of positive signatures found only an empty `correctOptionIds` array and four lazy staff-editor member accesses in `DxmIlFk3.js`; `DV4hE_NC.js` retains the guarded optional `async_hooks` helper. Neither is an embedded answer bank. Auth/learning data remains server-authorized. No genuine provider-secret values were available for exact comparison, and hosted CDN files were not audited. Earlier performance measurements retain their earlier build IDs; the new bundle scan is not a new timing or capacity measurement.

The CI classic guard fixes the reviewed Git blobs of `HomePageClassic.vue` and its actual `pages/index.vue` route at commit 413ad4a, with no automatic baseline refresh. Its new browser suite passed **eight RU/KK viewport cases** on cb3895e4 and checked the existing structure, selected computed styles, nine historical links, six format cards, grid columns, logo and overflow. RU 1440 and KK 360 screenshots were viewed. This is source/structural/style protection, **not pixel-diff**; see `docs/CI_CLASSIC_PRESERVATION.md`. The new GitHub run has not yet executed at this snapshot.

The separate keyboard-only learner pilot on Node4855f35b completed **6/6 stages**, 18 focused controls and eight genuine API mutations without mouse or focus bypass (`docs/KEYBOARD_LEARNING_PILOT.md`). **T079 remains not_run** because screen-reader smoke is still missing. AX data and accessible locators do not establish screen-reader speech or virtual-cursor behavior.

The current full test run also verifies encrypted backup/restore of schema **001–010**, including correlation metadata, and a populated **005→010** additive upgrade with unchanged old values, NULL new metadata, FK/integrity and immutable triggers. These are distinct from the earlier timed 001–005 CLI drill and old6aa read/refusal experiment. Neither old-runtime write rollback nor compatibility with schema010 is claimed as tested; B53/T083 remain not_run.

No new remote CI, hosted readiness, provider delivery or production deployment result is inferred from this package's local results. The following sections retain historical evidence with its original artifact and schema scope.

## Previous operational package evidence

That package's full local test command finished **124/124 passed, zero failures/skips, exit 0, 34.22 seconds** (`implementation-staff-workflows-124-tests.log`), including the populated schema005→009 upgrade regression. The preceding 123-test run also passed. Full ESLint and final sequential/prepush Nuxt typecheck passed. Two earlier runs stopped on Windows EBUSY while deleting an artificial negative-FK fixture; only its lifecycle was corrected, while real file-backed FK tests remain. The earlier typecheck with implicit-any errors in rollback scripts is also excluded from passing evidence.

These independent browser runs exercised Node **c76fd093-af00-4e6b-9b68-b69a40dc2480**, each on a separate synthetic database:

| Run | Actual outcome | Evidence |
| --- | --- | --- |
| Core portable launcher, port 3105 | Auth 6; contact 2 locales; learner 11; staff 13. Four suites passed, exit 0. | `artifacts/core-browser/e0a5a852-aed2-40aa-8217-8f39a92c4fdb/report.json`, 15:37:02 UTC. |
| Document operations, port 3103 | 20/20 checkpoints, exit 0, zero JavaScript errors. | `docs/DOCUMENT-OPERATIONS-PILOT.md`, `artifacts/document-pilot/report.json`, 15:36:18 UTC. |
| Organization report/reminders, port 3104 | 13/13 checkpoints, exit 0, zero JavaScript errors. | `docs/ORGANIZATION_REPORT_AND_REMINDERS.md`, `artifacts/organization-report/report.json`, 15:34:34 UTC. |
| Additional acceptance, port 3106 | Eight scenarios plus JavaScript check: 9/9, exit 0. | `docs/ACCEPTANCE_EXTRA_BROWSER.md`, `artifacts/acceptance-extra/report.json`, 15:57:14 UTC. |

Authentication and MFA were actual UI flows. The document pilot began with users and a published synthetic version only: no enrollment, completed lesson, practice, attempt, template, credential or render job was seeded. Subsequent mutations used UI controls; SQL observations were SELECT-only. Specialized fixtures explicitly identify seeded facts and controlled time changes. Contact responses were intercepted for validation/error/retry, so those locale checks do not prove actual CRM delivery.

The learner suite was additionally rerun against the final clean Node artifact **4855f35b**: **11 passed, exit 0**, finished 15:52:24 UTC. Evidence is `artifacts/core-browser/e27e7ba2-5360-4350-8bc7-4166c8ad961a/report.json`. The other suites retain their exact c76 scope; their JavaScript assets are identical to the final build.

The extra run closes T008/T010/T020/T051/T062/T063 locally: KK/Astana/program survives selection and authentication; home/catalog preserve an active attempt; controlled expiry of the actual session yields 401 and reauthentication restores answers/deadline; a real order remains created without access after `/success?paid=true` returns 404; verification exposes exactly six safe fields with private-cache/robots headers; bad-token enumeration reaches a uniform 429 after 29 invalid 404 responses. Session expiry used a controlled test timestamp, not a wait for its full lifetime.

The final repeated extra run also closes T033/T040 with a new independent 390px Chromium context and an actual separate server session. Completed lesson/pinned version and the active form/order/deadline/answers matched the first session, which remained usable. This proves independent browser-session continuity; it does not claim two physical devices were used.

## Prior operational requirements evidence

B42 now has direct report evidence beyond the earlier invoice example. Independent access, required-learning, practice, latest-exam and document states use real denominators. Browser checks cover an empty organization, 71 assignments, 62 members, 60 invitations, paging, filtered CSV and RU desktop/KK 360px. Service tests cover 520 assignments and more than 100 organizations. CSV is complete up to 5,000 rows; larger results return explicit 413 with a real count and filter guidance. A single program above that bound still needs a future export job. No silent truncation or invented compliance percentage is accepted.

B43 now has a complete local operator pilot: role grant/revoke, practice selectors/evidence, server exam, queue failure/retry, repair, owner PDF/QR, withdrawal/reissue, incident acknowledgment/recovery, support-note correction history, batch preview/confirmation with duplicate reuse, and intake close/reopen. These support its local passed status. Provider reconciliation, actual issuer templates and external production channels remain separate limitations.

The pilot found two product defects. The instructor form required an undiscoverable raw lesson ID; it now selects assignments and practical lessons from the exact pinned version. PDF flattening left six dangling annotation references; cleanup now removes unresolved/form-widget references while preserving valid annotations and painted content. Seven repair tests and the repeated full pilot verified the fix. Two actual downloaded PDFs passed pypdf/Poppler structure checks, Cyrillic extraction and QR image decoding; both PNGs were visually reviewed. The original file hash and academic history survive replacement. These documents are explicitly synthetic and have no qualification validity.

Migration 008 stores intake separately from immutable program history. New self/staff/org enrollment, order and invoice creation check current state inside their transaction. Prior idempotent replays and accepted learning/invoice obligations continue. Three tests and the UI pilot verified closing intake, completing existing work and reopening it. Old writers that ignore intake are unsafe for live rollback.

B44 has working in-app reminders with explicit planned dates, IANA/DST, offsets, revisions, deduplication, cancellation, missed windows and opt-out. Nine service tests and the report/reminder browser verify this. No legal certificate-validity period is calculated. Actual SMTP delivery and Vercel scheduling remain unconnected/unverified, so the complete external-operation requirement stays blocked.

B52 has bounded incident detection, redacted counters, scoped in-app alerts, MFA acknowledgment, source-aware resolution and retry. The pilot exercised the operator path; seven tests include rejected-webhook audit without raw payloads/signatures. No expected external alert was sent to the owner. B52/T081 remain blocked for recipient/channel configuration and production failure injection. T052/T065 pass only for implemented internal sandbox/transport and visible durable incident behavior; an official payment provider remains unconnected.

## Prior build, HTTP and privacy evidence

Final sequential cold Node **4855f35b-6913-4002-a0fa-7c87ad77c0a7** and cold Vercel **ca57b6d3-18bb-45e9-93e2-afaf737785b9** both exited 0 and passed the strict gate: **550 public HTML plus one Google verification file, 15,797 asset references, one build ID and zero missing resources**, with no private prerender.

The earlier Node c76 directory acquired an unrelated metadata file during parallel Nuxt typecheck and failed its artifact-ID gate. That failed gate was retained and closed by the new sequential build. Functional browser evidence still names c76; its JavaScript files have identical paths/SHA to final Node/Vercel. Browser success and artifact integrity are separate evidence, not interchangeable claims.

Full HTTP inventory repeated on c76 at **15:39:21 UTC** passed **550 public pages, 10 private header checks, 6 true404s, sitemap550, canonical/hreflang and robots** (`artifacts/seo/http-contract-all-report.json`). Local responses do not prove hosted CDN isolation or production DNS/aliases.

The final audit `artifacts/privacy/public-bundle-audit-4855f35b-ca57b6d3.json` checked **89 JavaScript and 5 CSS files per artifact**, identical paths/SHA, 89 parsed ASTs and 1,204 public text files each. **102 exact private canaries had zero matches**; maps were absent and both artifact identity gates passed. Positive `correctOptionIds` signatures are an empty array and four lazy staff-editor member accesses, not embedded answers. Privileged APIs enforce authorization independently of downloadable UI. No real-provider secrets were available for exact comparison; this is not universal proof against transformed secrets.

The previous explicit-noindex build remains separate evidence (`docs/PREVIEW-NOINDEX-QA.md`, 84765834): 550 public pages, sitemap550, 15,770 resolved references, noindex/nofollow and robots Disallow:/. Default indexable checks remain strict. Noindex preview intentionally disallows crawling; robots is not authentication.

## Prior design and performance evidence

The valid visual review retains 52 responsive pages, eight HomePageClassic comparisons, six explicitly identified CSS-zoom checks and two keyboard/name/error checks. HomePageClassic and the brand were preserved.

The earlier 6aa performance experiment measured 12 public baseline/after navigations, one terminal exam view and 100 GET requests at concurrency10, all successful. Health p95 was 76.63 ms and catalog p95 108.07 ms. Its terminal shift near 0.18 is a historical finding that was subsequently reproduced and addressed.

`docs/LMS_NAV_LAYOUT_LAB.md` records two narrow CSS corrections. Candidate718c3f06 passed **32/32 GET-only navigations**; 16 controlled font-swap cases stayed within1 CSS pixel, and 101 loaded KK header widths retained their geometry. Natural RU390 terminal shift changed0.18025→0.00055; KK360 changed0.19926→0.00884; final maximum observed sum was0.03620. These are single-run local observations and controlled geometry checks, not field CLS/INP/CWV, active-exam load or remote capacity evidence. Native zoom, screen-reader acceptance and human KK/SME review are not certified by these checks.

## Prior backup and rollback evidence

Five backup tests passed with actual restoration of schema001–009 tables/rows, including reminders, intake, support notes and batches (`implementation-backup-nine-migrations.log`). The separately timed older CLI restore on schema001–005 remains historical; its timing is not relabelled as a new remote restore.

`docs/ROLLBACK_DRILL.md` and `artifacts/rollback/report-2026-09-07T15-44-09-935Z.json` record **15/15 local operations**, exit0. Old6aa completed six protected GET requests with the current synthetic HTTP session on a schema001–009 copy. Of43 tables,42 were unchanged; only the expected Better Auth rateLimit counter changed. Eight unsupported-feature/unknown-migration/checksum cases were blocked before old-runtime startup.

Live write rollback remains blocked: old6aa ignores features006–009. The runtime diagnostic seeded current schema, copied/restored it and reapplied migrations. A separate actual populated schema005→009 upgrade regression subsequently passed **1/1, exit 0**, preserving data, documents and constraints (`tests/migration-upgrade.test.ts`, `artifacts/migration-upgrade-tests.log`). These are distinct upgrade/read/restore tests. B53/T083 retain not_run for the complete rollback scope, with live write rollback blocked and the successful local components stated explicitly. Production/remote RTO/RPO remain unverified.

## Prior hosted release scope

The operational release commit **cc6b662535853196fd2f6404f7c2439c33f69ee3** was pushed and [Quality34141297253](https://github.com/shalinivan9669/sertificate/actions/runs/34141297253), job101803650908, completed **success**. Its downloaded logs confirm **124/124 tests, zero failures/skips,22.44s**, audit0, install/lint/typecheck, Node/SEO/assets, installed Chromium and all four isolated browser suites (auth6/contact2 locales/learner11/staff13, CI runtimeab79c6a4), followed by the Vercel build. This is remote CI on a synthetic local SQLite database, not a remote production database test.

Its [Vercel deployment2mSGsQqBF](https://vercel.com/shalinivan9669s-projects/sertificate/2mSGsQqBFsn5VuexFzCq1fNgxKD1) reached **Ready in1m18s**. The authorized browser opened the classic homepage, catalog20 and ISO9001RU/KK with request-only pricing on [the cc6b662 preview](https://sertificate-czv7s2iyb-shalinivan9669s-projects.vercel.app/). The project's Storage page still lists no databases; the Turso terms step remains pending owner input. These public-page observations do not close hosted LMS readiness, email or CRM delivery. Subsequent documentation-only commits do not alter this runtime evidence.

The prior [GitHub Quality run 34133936157](https://github.com/shalinivan9669/sertificate/actions/runs/34133936157), job 101780349554, passed for commit **8f68**, including **93/93 tests, zero failures/skips, 12.67 seconds**. The earlier [run 34132591085](https://github.com/shalinivan9669/sertificate/actions/runs/34132591085) also passed for1e23fc58. These historical results apply only to their respective commits in [draft PR2](https://github.com/shalinivan9669/sertificate/pull/2); the current operational package has its own result above.

The latest prior 8f68 [Vercel preview](https://sertificate-cw86taz0z-shalinivan9669s-projects.vercel.app/), deployment FH8NyJt2YuKJnsPSkHMfyF1Tpvwr, reached Ready in 1m12s. This is separate from both the earlier public-page observations and the current local operational artifacts.

The earlier protected Vercel preview reached Ready and four public pages were viewed. Its KK cabinet showed request-error/retry, /api/ready was blocked by the client and external requests encountered Vercel SSO. These are not hosted account/readiness passes; protection was not bypassed. The existing production public host answered a read-only redirect/200 check; main was not updated with this LMS.

At that historical snapshot, production DB/Auth/SMTP and hosted readiness were still missing. The current production release, connected database, verified public readiness and accepted email-OFF constraint are recorded at the top of this document. Region/retention, official payment adapter, authenticated CDN isolation, cron execution, approved materials/templates and human KK/SME review retain their stated limits. The owner authorized deployment and confirmed the center's permissions; these matrices do not reopen legal-entity permission checks or infer authorization for real payments or mass mail.
