# Предпросмотр уроков и сохранение прогресса

## Проверяемая задача

При переключении A → B → A ответы чтения уроков могут завершаться в другом порядке. До исправления более ранний ответ A перезаписывал уже полученную новую ревизию A. Если слушатель переходил к B во время сохранения A, сообщение «Завершение урока сохранено» появлялось у незавершённого B. Это неверное отображение; факт завершения B на сервере не создавался.

Оба случая воспроизведены на исходном `a007577` в `artifacts/learning-navigation-race-review-report.json` 8 сентября 2026, 11:27:55 UTC. Проверка исполняла фактический TypeScript из SFC с настоящими Vue refs и управляемыми обещаниями API. Это доказательство порядка асинхронных операций, а не браузерного рендера или удалённого сервера.

Предпросмотр нужен, чтобы редактор мог прочитать текущий текст урока и расшифровку видео до сохранения, согласования и публикации. Отдельный общий компонент сохраняет обычный экранированный текст и отображение медиа. В редакторе внешние файлы представлены описаниями: несохранённые адреса не вызывают сетевую загрузку. Полный банк вопросов не является частью предпросмотра урока.

## Изменение поведения

Каждое чтение урока имеет собственный номер запроса. Устаревшие ответы, ошибки и завершения загрузки не заменяют текущее представление. Сохранение связано с исходным уроком и просмотром. Страница использует уже полученный ответ PUT с актуальным назначением и ревизиями; дополнительные GET назначения и урока больше не нужны. Это два устранённых HTTP-запроса на завершение урока, без утверждения об измеренной стоимости Turso.

На телефоне прежний список из 16 уроков занимал около 1970px по высоте. После выбора второго модуля материал оставался примерно на 2523px ниже верхней границы viewport 390×844, фокус — на кнопке содержания. Теперь явный выбор переводит фокус к заголовку загруженного урока; ссылка «К содержанию» / «Мазмұнға» возвращает к именованному заголовку списка. Первичная загрузка, сохранение и повторный запрос сами по себе не перемещают фокус. Новое действие пользователя или размонтирование отменяет устаревший перенос. Отступ прокрутки 1rem оставляет место для рамки фокуса.

Первое испытание кандидата af32 после снимка всей страницы показало заголовок на -8px по вертикали. Это не выдано за доказанный обычный сценарий: возможное влияние screenshot/reflow отделяется в финальном измерении до и после снимка. Исходная проблема с материалом ниже 2523px измерена непосредственно после выбора.

## Область приёмки

Пункт B27 исходного backlog требует авторизованного подтверждения практики, клавиатурной доступности, альтернатив изображениям и расшифровок видео; чтение или таймер не подтверждают компетентность. Отдельный T079 требует также фактического screen reader smoke. DOM/AX-снимок или работа с клавиатурой не заменяет последнее требование.

Содержательный RU-пакет на 16 уроков хранится отдельно в `.data/internet-learning-pilot-20260908`. Он остаётся неутверждённым черновиком с неизвестной длительностью. Техническое испытание его представления не утверждает образовательную программу. Если для локального сценария создаётся TEST-копия с явно тестовым объёмом, изменения относятся исключительно к синтетической локальной fixture; исходный пакет и production не меняются.

## Доказательства

- Серверные проверки `tests/core-domain.test.ts`: 21/21, 0 failed/skipped, 14.0509484 s; `artifacts/lesson-media-domain-tests.log`. Новая проверка проходит реальный локальный путь draft → review → publish → enrollment → lesson для разрешённого HTTPS-источника изображения, видео и вложения. Сохраняются alt/транскрипт/описания, само чтение не завершает урок. Без alt изображения или расшифровки видео версия остаётся черновиком; неизвестный хост отвергается. Внешние файлы при этой серверной проверке не запрашиваются.
- `tests/learning-navigation.test.mjs`: 12/12 сценариев фактических обработчиков, включая перестановки ответов и отмену фокуса. Первая серия из семи сценариев дала 1 passed / 6 failed на прежнем исходнике и 7 passed после исправления; затем добавлены проверки фокуса. Это управляемый транспорт и настоящие Vue refs, без подмены такого результата браузерным или серверным доказательством.
- Полный `npm test`: **302/302**, 0 failed/cancelled/skipped/todo, **60.0425267s**, `artifacts/lesson-preview-full-tests.log`. Полные lint/typecheck прошли. Последующий отступ scroll-mt-4 меняет только CSS; точная окончательная версия отдельно проверяется в CI.
- Исторический реальный локальный пилот исходного dd526: `artifacts/actual-material-pilot/5136d18e-9d5c-4f93-be27-13bf38341c8a/report.json`, 9 этапов и открытая mobile-проблема. Все 16 тел уроков совпали с пакетом; 8 текстовых завершений дали 8/16, learner self-practice и экзамен отвергнуты сервером. Сотрудник через настоящий MFA/UI подтвердил один TEST-кейс: 9/16, экзамен всё ещё закрыт. Нет попыток, документов, платежей, заказов или заявок; сессии завершены. Два ранних сбоя самого harness (ожидание первого render и имя таблицы) сохранены отдельно.
- Первый общий browser-прогон af32 прошёл classic/auth/contact/context/learner и предыдущие staff-сценарии, затем остановился на новом тестовом локаторе: доступное имя нормализует завершающий пробел синтетического заголовка. Ожидаемое имя исправлено, runtime не менялся; `artifacts/lesson-preview-core-browser.log` не объявляется полным успешным прогоном.
- Финальная Node-сборка **61cfe878-7e4f-4baa-948a-6fc9130d43df**, `artifacts/lesson-preview-node-build-final.log`, успешно собрана; SEO550public, assets551HTML/18672refs/0missing/одинbuildID.
- На ней actual-material pilot **11/11**, 0findings/0JSerrors, **11:50:51.409–11:51:08.612UTC**: `artifacts/actual-material-pilot/a644c3c3-04ed-4e7f-83ba-279c85af3a84/report.json`. Все16уроков точно совпали при1440/390px; фокус/возврат к содержанию доступны, заголовок и рамка полностью видны. Восемь текстовых завершений и одно настоящее MFA/UI-подтверждение TEST-практикума дают9/16; экзамен всё ещё запрещён.0попыток/документов/платежей/заказов/заявок,0сессий после выхода. Исходный закрытый пакет не изменён. Масштаб CSS2x — только симуляция переразметки, без утверждения о нативном browser zoom или screen reader.
- На том же61cfe повторены learner+staff browser: **2/2 suites**, learner11/staff17, завершение **11:53:15.074UTC**, `artifacts/core-browser/f6af18ea-b2b5-41e3-9786-aaa1ce446fc2/report.json`. Предпросмотр RU/KK — по5этапов, **11:51:43.839UTC**, `artifacts/lms-browser/lesson-preview/report.json`,0domainwrites/0externalmediarequests. Список сохранённых версий до/после совпадает. Во всех сценариях только изолированные TEST-пользователи и локальная БД.
- Пакет опубликован в составе `f5c8a0899a3d193599298d781ccafe08fa65dac5`; точные CI и production-данные приведены ниже. Прежняя публикация a007577 сохраняет свою историческую область проверки.

## Последующая проверка браузеров и Narrator

GitHub Quality `34223816421`, checkout `1c0c74a`, прошёл 302 unit tests (42.238974351 s), lint/typecheck/audit/Node build/SEO/assets, но завершился failure в новом helper `keyboardReach` после редактирования текста на 360px. Условие полной видимости сфокусированной кнопки закрытия не выполнилось за 5s. Отчёт не содержал геометрии; общий failure screenshot ошибочно показывал последнюю страницу reviewer вместо редактора. Дальнейшие шаги этого запуска были skipped.

Следующий commit `f5c8a08` добавляет только диагностические измерения фокуса/геометрии и правильный failure screenshot; условие, таймаут и приложение не изменены. Локальный Chromium 149.0.7827.55 на прежней сборке 61cfe878 прошёл весь staff suite и RU/KK preview со строгой проверкой видимости. Неизменяемая копия: `artifacts/preview-focus-1228-20260908-121652/`; общий результат `artifacts/core-browser/859da67d-4a5c-4753-a5de-1a27085e3bb0/report.json`. Измерения показывают плавную прокрутку после Tab; это не доказывает причину Linux-сбоя. Установка соответствующего Chromium 145 на Windows не завершилась, собственный установщик остановлен. Новый Linux Quality `34225381057` фиксируется отдельно; успешный повтор не будет выдан за установленную причину или исправление первоначального сбоя.

Этот [Quality 34225381057](https://github.com/shalinivan9669/sertificate/actions/runs/34225381057), job102058098018, завершился **success** на точном f5c8a08. 302/302 tests за32.996258411s, 0 failed/cancelled/skipped/todo; lint/typecheck/audit0 vulnerabilities; core6 (staff17), analytics20, privacy35, refund10, journey10 и workspace9 — passed. Node `3c27354c-1a59-4049-9e99-8e809d8c9ea3` и Vercel `dda28c5b-306d-4f7e-bef4-359ddfbf99ac`: по550public/551HTML/18672asset refs/0missing. Не пропущено обязательных шагов. Первоначальный нестабильный сбой сохраняется как необъяснённый; его исправление не заявляется.

После CI точный commit отправлен в main. [Vercel production 5YsNHuzjPJJAn6FEsFWZq8392RJp](https://vercel.com/shalinivan9669s-projects/sertificate/5YsNHuzjPJJAn6FEsFWZq8392RJp) Ready **12:30:11 UTC**,1m28s, main/f5c8a08, **www.otcenter.kz**. Положительный публичный GET smoke **8/8** завершился12:30:29UTC: главная/health/ready/auth/catalog/analytics и корректный apex→www redirect. Отчёт `artifacts/production-public-smoke-f5c8a08-report.json`. Это не hosted-тест входа в кабинеты: настоящие учебные материалы ещё не опубликованы, каталог показывает20цен по запросу, почта/платежи выключены. Последующее дополнение только Markdown сохраняет этот runtime без изменений.

Фактическая попытка T079 закончилась до запуска Narrator: 12:13:23.867–12:15:48.357 UTC, собственный UUID стенд `c9aaaaf1-520f-4033-b0e9-157f9b5327cb`, Node 61cfe878. Read-only inventory через штатный `@oai/sky` был доступен, однако первое чтение окна Chrome остановила автоматическая проверка безопасности Computer Use: текущий URL нельзя было достаточно надёжно определить. Отказ не обходился другим UI API. Нативных нажатий, наблюдаемых utterances и результатов scan mode нет; **T079 not_run**. Собственные Chrome/server/runner завершены, Narrator не работал ни до, ни после; в сохранённой локальной БД осталась одна синтетическая сессия, logout не заявляется. Подготовительный BetterAuth API-вход не является screen-reader проверкой. Отчёт: `artifacts/narrator-smoke/c9aaaaf1-520f-4033-b0e9-157f9b5327cb/report.json`. Production и реальные данные не затрагивались.
