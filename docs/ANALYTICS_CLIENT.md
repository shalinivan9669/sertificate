# B48 — добровольная статистика, клиентская часть

События включаются только после отдельного выбора «Разрешить статистику» на `/privacy` или в кабинете и только при положительном `/api/v1/analytics/config`. Выбор не связан с маркетинговыми уведомлениями. Без cookie согласия обычные страницы и события не вызывают ни POST аналитики, ни GET её конфигурации. Сам блок согласия читает GET конфигурации, чтобы показать доступность и установленное окно хранения; это не отправка статистического события.

`ot_analytics_consent=analytics-v1` — cookie без срока истечения, `SameSite=Lax`, `Path=/`. Отзыв очищает cookie, общий выбор в текущем приложении и очередь, отменяет активный запрос и отложенный повтор. Временная очередь хранится только в памяти: максимум 20 событий, не более одной активной отправки, срок жизни 60 секунд. При сбое возможен один повтор с тем же UUID. Запись в localStorage/sessionStorage, отдельный visitor ID и внешние платформы не используются. Событие, уже принятое сервером до отзыва, не отменяется браузером.

Схема событий и измерений общая с сервером: `shared/analytics.ts`. Разрешены только известная программа, RU/KK, город из списка, online/classroom/onsite и b2c/b2b. Данные формы, имя, контакты, полные URL, query, ID пользователя/назначения/урока, ответы и баллы экзамена не передаются. Непроверенные строки удаляются клиентским allowlist; сервер повторно проверяет запрос независимо от клиента.

| Событие | Реальный триггер |
| --- | --- |
| `program_view` | Открылась карточка известной программы после загрузки; также историческая SEO-страница CoursePage с известным направлением. Неизвестный маршрут/404 события не создаёт. |
| `selection_start` | Первое изменение формы подбора или переход между её шагами. Одно на экземпляр подбора. |
| `selection_complete` | Пользователь открыл результат подбора с известным выбранным направлением. Одно на экземпляр подбора. |
| `contact_click` | Нажатие консультационной CTA карточки или телефонной/email ссылки в контактах и SEO-карточке. |
| `lead_form_start` | Первый фактический input в форме обращения. В событие не копируется содержимое полей. |
| `checkout_view` | Загружена существующая выбранная версия на странице условий заказа. Оплата/успешность заказа этим не утверждаются. |
| `lesson_open` | Существующий урок успешно загрузился в UI. Повторное обновление того же урока после сохранения прогресса не считается новым открытием. |
| `support_open` | Нажатие «Связаться с OT Center» в LMS или «Помощь специалиста» в подборе. |

Согласие и отказ сами по себе не создают события. События до согласия не сохраняются для последующей отправки. Открытие карточки после согласия создаёт новое реальное событие просмотра; нажатие «Разрешить» не воспроизводит прежние посещения. Эти события не подтверждают академический результат, платёж или выдачу документа: такие факты принадлежат серверу.

Кнопки доступны независимо от авторизации на RU/KK privacy и отдельно в кабинете/настройках уведомлений. Отказ доступен и при выключенном серверном сборе. При ошибке GET отображается повторная проверка настроек; новое согласие недоступно до получения валидной конфигурации. Текст показывает **окно хранения в настройках** и отдельное серверное задание очистки; мгновенное удаление или подключённый production cron не обещаются.

Фактические локальные проверки на этапе реализации: `tests/analytics-client.test.ts` **6/6 passed, exit 0**, 381 ms (`implementation-analytics-client-tests.log`); focused ESLint всех изменённых UI/transport файлов и теста — exit 0. Проверены отсутствие отправок без согласия, удаление произвольных/личных/академических полей, максимум один повтор с тем же событием, предел очереди, abort+очистка при отзыве, отмена запланированного повтора и отбрасывание событий старше минуты.

Эти шесть проверок относятся к транспорту; полный фактический браузерный прогон всех восьми взаимодействий описан ниже. Negative HTTP/log privacy-аудит ведётся отдельно в `docs/RUNTIME_PRIVACY_AUDIT.md`. Keyboard-only прогон Node4855 предшествовал этой функции и не переименован в проверку новых блоков согласия. Production включение, фактическое расписание очистки и policy approval требуют собственных подтверждённых настроек; по умолчанию сбор выключен.

## Отчёт администратора

`pages/admin/analytics.vue` читает `/api/v1/admin/analytics?days=7|14|30`. Ссылка видна только admin; доступ к данным независимо проверяет сервер по роли и MFA. Whitelist возврата после входа разрешает только предусмотренный маршрут `/admin/analytics` и его KK-вариант. Период применяется кнопкой «Обновить отчёт»; автоматического polling нет.

Таблицы содержат человеческие RU/KK-названия всех 8 клиентских и 11 серверных типов. Они показывают количество событий после исключения повторных отправок, а не уникальных посетителей. Добровольные действия браузеров и подтверждённые сервисные операции относятся к разным группам участников; отношение этих чисел не объявляется конверсией. Отчёт прямо сообщает, что агрегат может включать тестовые/sandbox действия окружения, без отдельного разделения по каналам.

Отображается именно возвращённое сервером UTC-окно с включённой начальной и исключённой конечной границей. Если срок в настройках меньше выбранных 30 дней, UI сообщает ограничение и фактическое число дней. При выключенном сборе исторические данные подписаны как ранее сохранённые; пустой отчёт имеет отдельное сообщение. Loading/error/retry, переход к входу для 401 и проверке MFA для 403 присутствуют. Focused ESLint страницы, навигации и whitelist фактически прошёл `exit 0`. Прямой вызов `safeLmsReturnTo` через Node/tsx подтвердил 3 разрешённых RU/KK/query адреса и отказ для 4 внешних/неизвестных/encoded назначений; эти 7 assertions не добавляются к счётчику `npm test`. Runtime-страница подтверждена положительным локальным browser-прогоном ниже; пустое/disabled состояние отдельно этим прогоном не проверялось.

## Полный actual browser QA — выполнен

На Node **cb3895e4-c572-4c0d-8891-2345381742f4**, 2026-09-07 **16:55:55.389 UTC**, выполнено **16/16 проверок, обычный exit 0, 0 ошибок JavaScript**. Harness `tests/analytics-browser.mjs` создал отдельную копию артефакта, порт 3109 и новую `.data/analytics-browser-2bd3d1af-f545-4e05-bfa5-550b22aafd59/e2e.sqlite`. `tests/analytics-browser-fixtures.ts` создал только синтетические аккаунты и опубликованные free/paid тестовые версии; до UI таблицы событий, назначений, прогресса, попыток и заказов были пусты. Во время seed сбор был выключен; во время браузерной проверки он включён только локально, с окном 14 дней. Внешние сервисы отключены.

Фактическая последовательность: явное согласие в privacy → SEO-карточка Астаны и LMS-карточка (`program_view`) → первый переход и все шаги подбора (`selection_start`, `selection_complete`) → консультационная CTA (`contact_click`) → первый ввод контрольного текста в контакты без отправки заявки (`lead_form_start`) → ссылка помощи LMS (`support_open`) → выбор paid-версии и просмотр условий без заказа/платежа (`checkout_view`) → настоящий вход, запись на free-версию через UI и загрузка урока (`lesson_open`). Каждое из восьми имён наблюдалось в настоящем POST после соответствующего UI-действия, получило HTTP 200 с `accepted=true`. Тест не вызывает POST аналитики сам и не подменяет ответы.

Получено **12 клиентских POST / 12 UUID**: пять реальных просмотров программы и по одному событию остальных семи типов. Read-only проверка SQLite сопоставила каждое имя/UUID/JSON с отправленным payload. Контрольный текст формы, email, ID пользователя, урока и назначения, пароль отсутствовали и в передаче, и в сохранённых измерениях. Настоящие запись на обучение и завершение урока дополнительно создали **два отдельных серверных события**: `enrollment_activated` и `lesson_completed`. Пустые таблицы обращений и заказов подтвердили, что просмотр формы/условий не отправлял заявку и не создавал оплату.

Свежий браузер до согласия посетил каталог, программу и изменил подбор: **0 POST аналитики, 0 GET конфигурации, consent-cookie отсутствовал**. Явное согласие создало ровно требуемый `analytics-v1`, session-cookie (`expires=-1`, `SameSite=Lax`, `Path=/`), без отдельного события согласия. Отзыв на KK privacy в окне 360px удалил cookie; последующие открытия KK-программы и ввод в контакты не создали новых POST.

Отдельный браузер администратора открыл защищённый отчёт без входа, перешёл по настоящему `returnTo`, выполнил парольный вход и **настроил и подтвердил настоящий TOTP через UI**. После MFA административная ссылка открыла две таблицы, все **19 строк**, явно разные группы участников и отсутствие показателя конверсии. Ручные запросы 7/14/30 дней вернули фактические UTC-окна 7/14/14; ограничение 30→14 было показано в UI. Слушатель получил 403 от report API и не увидел административную ссылку. RU 1440px и KK 360px снимки просмотрены; горизонтального переполнения KK-отчёта не обнаружено.

Доказательства:

- `artifacts/analytics-browser/2bd3d1af-f545-4e05-bfa5-550b22aafd59/report.json`: 16 passed, build ID, 8 имён, 12 UUID и UTC-границы запросов.
- В той же папке: `report-ru-1440.png`, `report-kk-360.png`, `withdrawal-kk-360.png`.
- `implementation-analytics-browser.log`: последний полный успешный запуск.
- `node --check tests/analytics-browser.mjs` и focused ESLint обоих новых тестовых файлов прошли.

Первый ограниченный запуск `3c470c67-f932-4ae6-984d-2cad099480d2` остановился на излишне точном тестовом label-селекторе роли; screenshot подтвердил нормальный второй шаг подбора. Селектор стал учитывать существующий label с текстом option. Этот прогон сохранён как failed evidence и не включён в 16/16. Приложение для исправления не менялось. Общая TS-проверка ранее обнаружила nullable аргумент cookie decoder: добавлена явная проверка типа, вошедшая в проверенный cb3895.

Повторение из корня репозитория, с завершённым Node artifact и соответствующими ему исходниками fixture/миграциями:

```powershell
$env:NODE_ENV = 'test'
$env:OT_ALLOW_TEST_SEED = '1'
$env:OT_ANALYTICS_ARTIFACT_OUTPUT = 'C:/path/to/completed/.output'
# BROWSER_PATH можно задать явно; без него применяется выбор для текущей платформы.
Remove-Item Env:VERCEL,Env:VERCEL_ENV,Env:TURSO_DATABASE_URL,Env:TURSO_AUTH_TOKEN -ErrorAction SilentlyContinue
node tests/analytics-browser.mjs
```

Скрипт каждый раз копирует готовый артефакт в новую UUID-папку, запускает свой процесс и Chrome, затем закрывает их в `finally`. Порт 3109 после выполненного прогона свободен. Общие `.output`, `.vercel` и `.data/e2e.sqlite` не изменялись; сборка Nuxt тестом не запускается. Сценарий подтверждает выбранные локальные UI-пути и положительные payload, но не заменяет negative error/log аудит, production/hosted проверку, реальную доставку или расписание очистки.

## Исправление выбора браузера после Linux CI

Для commit **4b7fcc66** [GitHub Quality 34146794421, job 101820468053](https://github.com/shalinivan9669/sertificate/actions/runs/34146794421/job/101820468053) успешно прошли 144 теста и пять основных браузерных suites, но шаг аналитики завершился ошибкой до запуска браузера: `tests/analytics-browser.mjs:93` без `BROWSER_PATH` подставлял `C:/Program Files/Google/Chrome/Application/chrome.exe` на Linux. Поэтому этот CI run **не является полностью успешной проверкой** аналитического сценария.

Исправлен только тестовый launcher. Явный `BROWSER_PATH` сохраняет приоритет; системный Windows Chrome выбирается только при `process.platform === 'win32'` и существующем файле; в остальных случаях используется `chromium.executablePath()` из Playwright. В JSON-отчёт добавлены `browserSelection` и `browserPlatform`. Проверены другие новые точки входа из `quality.yml`: `runtime-privacy-audit.mjs` и `classic-browser.mjs` уже имеют Playwright fallback, а `run-core-browser.mjs` передаёт переносимый `BROWSER_PATH` всем пяти дочерним suites. Их код и отдельные локальные pilots не изменялись.

После исправления выполнен полный локальный повтор на сохранённом `.data/analytics-release-node-d1402951-965b-45e7-997a-3cddd0e48962/.output`. Его фактический build ID из `latest.json` — **c4d9bf39-890d-4f44-8562-0075ce696d08**, а не идентификатор в имени внешней папки. Прогон **2026-09-07 17:29:37.655 UTC**: **16/16 passed, обычный exit 0, 0 ошибок JavaScript**, 12 POST/12 UUID всех восьми клиентских типов, два подтверждённых серверных события и настоящий admin TOTP/report сценарий. `BROWSER_PATH` был намеренно удалён из окружения; отчёт подтверждает автоматический выбор `windows-system`, `win32`.

Доказательство повторного прогона: `artifacts/analytics-browser/cd309068-28df-4f6f-8999-5263e45b0859/report.json`, лог `implementation-analytics-browser-portable-rerun.log`; KK360-скриншот отчёта просмотрен. `node --check tests/analytics-browser.mjs` и `npx eslint tests/analytics-browser.mjs` завершились `exit 0`. Новая synthetic DB и копия артефакта находились в отдельной UUID-папке, порт 3109 освобождён. Runtime приложения, сохранённый исходный артефакт и внешние сервисы не изменялись.

Этот повтор подтверждает Windows-ветку автоматического выбора и отсутствие регрессии сценария. Исправленную Linux-ветку предстоит подтвердить следующим удалённым CI после отдельного push; локальный успех не подменяет этот результат. Коммит или push в рамках исправления launcher не выполнялся.

## Дополнение: доставка когорты принятых заявок — 20/20 выполнено

Основной исполнитель запустил расширенный `tests/analytics-browser.mjs` на завершённом Node artifact **74c7c762-fa2a-45f4-bb9a-ad844f14ff29**. Прогон завершился **2026-09-08 03:19:44.306 UTC: 20/20 passed, exit 0, 0 JavaScript errors**, с `windows-system` на `win32`. Проверены фактические JSON-результаты и RU/KK-снимки. Report: `artifacts/analytics-browser/9d8c1abc-4711-40b3-84f1-511509ff72ca/report.json`; лог: `implementation-lead-analytics-browser.log`.

Новый участок расположен после всех прежних проверок восьми UI-emitter, сохранённых events, отзыва согласия, настоящего admin MFA, 19 строк отчёта и UTC-окон. Прежние assertions не ослаблялись и снова прошли: 12 настоящих client POST / 12 UUID всех восьми типов; отдельные серверные enrollment/lesson transitions; отказ learner; исходные окна 7/14/30 с ограничением хранения. Поле `leadCohort` соответствует `LeadCohortReport` из `shared/analytics.ts` и фактически отображается компонентом `LmsLeadCohort`.

Сначала подтверждены пустая когорта и отсутствие знаменателя для процента в реальном KK-интерфейсе. Затем существующий request context администратора выполнил ровно два новых POST `/api/amo-lead` в собственный локальный runtime: синтетическую B2C-заявку и B2B-заявку с разными idempotency keys. Обе получили 202/accepted. `OT_CRM_DELIVERY_ENABLED=0`; SQL в новом участке использовался только для чтения и подтверждения двух accepted записей без внешних CRM ID. Это **два HTTP-создания заявок**, а не доказательство отправки через формы UI. После этих операций серверный event-агрегат содержит четыре события: два прежних enrollment/lesson и два новых `lead_accepted`; клиентский агрегат сохраняет 12 событий.

Реальные кнопки обновления RU/KK-отчёта в 360px показали: `accepted=2`, `delivered=0`, `pending=2`, `notePending=0`, `invalid=0`, `deliveryRate=0`; в каждой аудитории по одной заявке. Проверены единица `accepted_leads`, совпадающее `[from,until)` окно, текущий `asOf`, видимые подписи и 0%, отсутствие контрольных контактов/названия компании во всём report DTO и тексте страницы, отсутствие горизонтального переполнения. `asOf` в отчётах: RU `2026-09-08T03:19:43.574Z`, KK `2026-09-08T03:19:44.055Z`. Отдельный массив `leadCohortChecks` содержит пустой случай и оба заполненных состояния. Финальная проверка JavaScript errors прошла после дополнения.

Снимки `lead-cohort-ru-360.png` и `lead-cohort-kk-360.png` из папки этого report просмотрены в исходном разрешении. Карточки когорты, длинные RU/KK-подписи, обе аудитории и нулевые доли видимы без наложения или обрезания значений. В прежней таблице серверных events заголовок правого столбца переносит последнюю букву на отдельную строку при 360px; это наблюдение о типографике, не горизонтальное переполнение или ошибка новых чисел. Runtime/CSS в рамках проверки снимков не менялись.

При подготовке `node --check tests/analytics-browser.mjs` и focused ESLint прошли с `exit 0`. Фактический расширенный прогон использовал отдельную `.data/analytics-browser-9d8c1abc-4711-40b3-84f1-511509ff72ca` и собственный runtime на 3109; проверяющий снимки не запускал дополнительных процессов, тестов или сборок. Исторические 16/16 и предыдущие удалённые CI-прогоны остаются отдельным evidence своих версий. Новый local 20/20 не является новым CI/hosted результатом, реальной доставкой CRM, first/last-touch атрибуцией или полной public/B2B воронкой. Нулевой delivered здесь соответствует намеренно выключенному внешнему каналу.

## Финальный повтор после исправления заголовка — Node 70a16ebb

Финальный artifact **70a16ebb-3355-431a-b18c-7b45f7f23560** повторно прошёл весь расширенный сценарий: **20/20 passed, exit 0, 0 JavaScript errors**, завершение **2026-09-08 03:30:18.400 UTC**, `windows-system` / `win32`. Обычный exit 0 подтверждён основным исполнителем для процесса 80468. Доказательства: `artifacts/analytics-browser/fa0632b7-7d1b-4d08-b0a2-c7aee6360dd5/report.json` и `implementation-lead-final-analytics-browser.log`. Предыдущий 74c7c762 с 20/20 и обнаруженным переносом заголовка остаётся самостоятельным историческим результатом выше.

На новой отдельной synthetic DB снова проверены все восемь реальных UI-emitter, **12 POST / 12 UUID**, отсутствие сбора без согласия, отзыв на KK360, learner denial и настоящий admin login/TOTP. Прежние event/count/19-row assertions выполнены до двух новых локальных HTTP-созданий заявок; эти записи снова не обозначаются как отправка форм через UI. После них RU/KK360 отчёт показывает **2 accepted / 0 delivered / 2 pending**, по одной заявке B2C/B2B, остальные счётчики 0 и долю доставки 0%; контакты не попали в DTO или текст. Пустой случай имеет `deliveryRate=null`. Текущий `asOf`: RU `2026-09-08T03:30:17.323Z`, KK `2026-09-08T03:30:18.167Z`.

Обе финальные картинки `lead-cohort-ru-360.png` и `lead-cohort-kk-360.png` в папке fa0632b7 просмотрены в исходном разрешении. Заголовки правого столбца «События» и «Оқиғалар» теперь целиком помещаются на одной строке в обеих таблицах; прежнее наблюдение о переносе последней буквы закрыто. Длинные строки данных и подписи cohort-карточек читаются без наложения или обрезания значений, горизонтального переполнения нет. Проверяющий менял только этот документ и не запускал дополнительные процессы/сборки. Изменение B2B honeypot входит в artifact, но его отдельную UI-приёмку этот analytics-сценарий не заменяет; внешний CRM по-прежнему выключен.
