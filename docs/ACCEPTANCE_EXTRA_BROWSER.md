# Additional acceptance checks — 2026-09-07

The eight scenarios below passed against the copied Node runtime `c76fd093-af00-4e6b-9b68-b69a40dc2480` at `http://127.0.0.1:3106`. The complete browser run on a fresh isolated database finished at **15:57:14 UTC**, exit 0, with **9 passed checkpoints** (eight cases and the JavaScript-error check), zero failures and zero JavaScript errors. Evidence is in ignored `artifacts/acceptance-extra/report.json`; screenshots are `artifacts/acceptance-extra/verification.png` and `artifacts/acceptance-extra/independent-session-390.png`.

The runtime's functional checks are qualified independently of its known extra generated-metadata file: these tests exercised the served application, and do not establish that this artifact passed the final single-build-ID asset gate. The parent release checks track that separately.

| Case | Actual action and result | Scope |
| --- | --- | --- |
| T008 | On the Astana course page, chose KK in the header, followed the program-selection CTA, completed direction/role/industry/format steps, followed the card, triggered unauthenticated enrollment, signed in through the KK form, enrolled and navigated to the KK cabinet. | `city=astana` survived selection and the auth `returnTo`; the selection cookie retained city, canonical direction and worker role. The server enrollment pinned the published synthetic **KK** version. City is selection context, not a newly invented enrollment database field. |
| T010 | Completed the real lesson through the UI, started an actual server attempt, saved an answer, used the home and catalog navigation, returned through the cabinet and the active-attempt resume link. | Enrollment ID, required progress, attempt ID, question form/order, deadline and saved answers were unchanged. |
| T033 | Opened a second independent Chromium context at 390 × 844 and signed in again through the real KK form. The server issued a different session ID for the same learner. | The enrollment retained the same published version, required progress was 100%, and the mobile lesson UI showed the completed lesson. This proves recovery between independent browser sessions; it does not claim a physical second-device test. |
| T040 | In the independent mobile session, followed the active-attempt resume link and loaded the server draft. Kept the original context open. | Attempt ID, question form/order, deadline and saved answers exactly matched the original session. The saved choice was selected in both UIs, the original session ID remained valid, and the mobile viewport had no horizontal overflow. |
| T020 | Read the current session created by the real sign-in, expired that exact session in the isolated test database, observed API 401, followed the KK login recovery link and signed in again. | The entire stored draft (`status`, deadline, revision, form JSON, answer JSON) was unchanged. No session or token was fabricated via SQL. This adds expiry/invalidation/relogin evidence; the separately recorded authentication suite covers sign-out/reset/verification. |
| T051 | Created an order through the actual server endpoint using explicit synthetic payment-terms configuration, then opened `/success?paid=true&orderId=...` without any webhook. | The URL returned a real 404, the order stayed `created`, its enrollment remained null, the learner's enrollment count stayed unchanged, and the active exam was still active. Payment provider was disabled. No checkout, webhook or external payment was sent. |
| T062 | An anonymous request verified the exact token of a synthetic PDF issued through the server's lesson/grading/template-review/render pipeline. Opened its QR landing page. | API returned exactly `serial`, `status`, `programTitle`, `issuedAt`, `revokedAt`, `registry`; no learner name, contact, internal user ID, answer/PDF bytes or verification hash. API had `no-store` and `noindex`. The landing page returned 200 with noindex metadata; this complete run also asserted HTML `Cache-Control: private, no-store, max-age=0` and `X-Robots-Tag: noindex, nofollow, noarchive`. |
| T063 | Requested random valid-format but nonexistent verification tokens without authentication. | 29 uniform `404 CREDENTIAL_NOT_FOUND` responses were followed by HTTP 429 on request 30, within the same limiter window as the preceding verification request. No useful record enumeration or personal details were exposed. |

`tests/acceptance-extra-fixtures.ts` creates a **new** `ot-extra-acceptance-*` temporary directory and `e2e.sqlite`, and uses `OT_E2E_FIXTURE_PATH` so it never overwrites the main learner fixture manifest. It publishes synthetic RU/KK and paid metadata, and creates the verification fixture through the real services: enrollment → lesson completion → saved answers → server grading → independently approved test PDF template → render. The resulting document is explicitly marked as a synthetic local test with no validity. Credentials and verification tokens remain only in the ignored `.data/extra-acceptance-fixture.json`.

`tests/acceptance-extra-browser.mjs` refuses ordinary, remote and Vercel database contexts. It uses separate browser contexts, fresh HTTP sign-ins and actual HTTP/UI actions. Only the targeted session-expiry timestamp is changed directly to control the negative scenario; all learner progress and attempt results are created through the application. The fixture, browser script and migration-upgrade test passed focused ESLint. No shared Nuxt build/prepare/typecheck was run during these copied-runtime checks.

An earlier run passed T008/T010/T020, then correctly received `503 PAYMENT_TERMS_NOT_CONFIGURED` when preparing the T051 order. A fresh isolated database/runtime was prepared with `OT_PAYMENT_TERMS_APPROVED=1` and a clearly synthetic terms version, while `OT_PAYMENT_PROVIDER=disabled` and all external-delivery flags remained disabled. The six initial scenarios then passed; the final fresh run added T033/T040 and passed all eight. No production configuration was changed.

The separate `tests/migration-upgrade.test.ts` passed **1/1**, zero skips, in 0.77 seconds total. It migrated a new temporary database using exact copies of migrations 001–005, populated clearly synthetic historical rows, captured all old tables and indexes/triggers, then applied current migrations 006–009. Every old row remained identical except the four expected migration-ledger additions; all nine new tables were empty. Foreign-key and integrity checks passed, immutable program/attempt/audit mutations were rejected, and rerunning migrations preserved the same data and ledger timestamps. The private snapshot remained in the temporary directory and was removed during cleanup. Evidence is `artifacts/migration-upgrade-tests.log`. This is a local forward-upgrade durability check, not a hosted or live rollback test.

These results establish local application behavior. They do not establish remote Turso behavior, hosted CDN headers, production capacity, provider reconciliation or real payment delivery.
